import { FETCH_HOW_TO_LOGIN, FETCH_HOW_TO_SUCCESS } from "./actions";

const initialState = {
  isLoading: false,
  data: "",
  error: "",
};



const reducer = (state = initialState, action) => {
  switch(action.type){
  case FETCH_HOW_TO_LOGIN:
    return 
  }
};
